# Core transcription backend
faster-whisper>=1.0.1

# Hugging Face analysis model (meeting summarization LLM)
# NOTE: transformers 5.2.0+ is required for the default Qwen analysis model.
transformers>=5.2.0
torch

# Enables device_map="auto" — lets Accelerate place the analysis model automatically.
accelerate

# Test suite
pytest>=8
